# Governance Frontend

前端已迁移为 `Vite + React + React Router + Ant Design 6`。

## 开发

```bash
npm install
npm run dev
```

默认端口读取 `.env` 的 `PORT`（当前为 `8001`）。

## 构建

```bash
npm run build
```

产物目录：`dist/`
