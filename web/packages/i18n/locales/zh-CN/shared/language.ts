import type { LocaleMessages } from '../../../types';

const languageMessages: LocaleMessages = {
    'language.en-US': 'English',
    'language.switch': '语言',
    'language.zh-CN': '中文',
};

export default languageMessages;
