import type { LocaleMessages } from '../../../types';

const accessMessages: LocaleMessages = {
    'access.adminOnlyButton': 'Admin-only Button',
    'access.noAdminPermission':
        'The current account does not have admin permission',
};

export default accessMessages;
